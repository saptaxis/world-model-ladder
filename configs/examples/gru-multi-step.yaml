arch: gru
arch_params:
  hidden_dim: 128
  num_layers: 1
  encoder_dims: [64]
  decoder_dims: [64]
prediction: delta
training_mode: multi_step
rollout_k: 10
data_mix: policy
data_path: SET_ME
state_dim: 8
action_dim: 2
lr: 0.001
batch_size: 64
epochs: 100
seq_len: 50
val_fraction: 0.1
run_dir: runs/
